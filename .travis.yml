language: generic
sudo: required
services:
    - docker


before_install:
    - docker build -t shahids12/docker-react -f Dockerfile.dev .


script:
    - docker run -e CI=true shahids12/docker-react npm run test -- --coverage